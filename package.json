{
  "name": "ng-portfolio",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "deploy": "powershell -Command \"git checkout main; ng build; mkdir ../temp-gh-pages; robocopy ./dist ../temp-gh-pages /E /XD browser; robocopy ./dist/browser ../temp-gh-pages /E; Copy-Item -Path ../temp-gh-pages/index.html -Destination ../temp-gh-pages/404.html -Force; git checkout gh-pages; Get-ChildItem -Path . -Force | Where-Object { @('.angular', '.git', '.idea', 'dist', 'node_modules', '.gitignore') -notcontains $_.Name } | Remove-Item -Recurse -Force; robocopy ../temp-gh-pages . /COPY:DAT /R:0 /W:0; git add .; git commit -m 'Deploy'; if (Test-Path ../temp-gh-pages) { Remove-Item -Recurse -Force ../temp-gh-pages }; git push; git checkout main\""
  },
  "prettier": {
    "printWidth": 100,
    "singleQuote": true,
    "overrides": [
      {
        "files": "*.html",
        "options": {
          "parser": "angular"
        }
      }
    ]
  },
  "private": true,
  "dependencies": {
    "@angular/common": "^20.3.0",
    "@angular/compiler": "^20.3.0",
    "@angular/core": "^20.3.0",
    "@angular/forms": "^20.3.0",
    "@angular/platform-browser": "^20.3.0",
    "@angular/router": "^20.3.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0",
    "zone.js": "~0.15.0"
  },
  "devDependencies": {
    "@angular/build": "^20.3.8",
    "@angular/cli": "^20.3.8",
    "@angular/compiler-cli": "^20.3.0",
    "@types/jasmine": "~5.1.0",
    "jasmine-core": "~5.9.0",
    "karma": "~6.4.0",
    "karma-chrome-launcher": "~3.2.0",
    "karma-coverage": "~2.2.0",
    "karma-jasmine": "~5.1.0",
    "karma-jasmine-html-reporter": "~2.1.0",
    "typescript": "~5.9.2"
  }
}
